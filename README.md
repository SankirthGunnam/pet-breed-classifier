# 🐶 Pet Breed Classifier (FastAI + Gradio)

This app classifies cat and dog breeds using a pretrained FastAI model trained on the Oxford Pets dataset.

## 🔍 How to Run

```bash
pip install -r requirements.txt
python app.py
```
